# Legacy

`index.html` here is the original hand-maintained page, retired when the
project moved to building `../index.html` from `../canon.toon`.

It embedded all content as four inline JS literals (`CATS`, `INTROS`, `DATA`,
`LINEAGE`) and rendered every card client-side. Kept for reference only — do
not edit, do not deploy. All content authoring now happens in `../canon.toon`
(see the root README).
